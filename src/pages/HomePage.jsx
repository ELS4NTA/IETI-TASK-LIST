const HomePage = () => {
    return (
        <section>
            <h1>Welcome to the Todo App</h1>
            <p>This is a simple todo app built using React.</p>
        </section>
    );
};

export default HomePage;
